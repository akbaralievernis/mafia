# Production settings
PORT=10000
NODE_ENV=production

# CORS (Frontend URL in Vercel/Netlify)
# Замените этот URL на реальный после деплоя клиента
CLIENT_URL=https://your-mafia-game.vercel.app

# Секреты (опционально, если потребуется JWT или БД)
SESSION_SECRET=super_secret_key_8347
